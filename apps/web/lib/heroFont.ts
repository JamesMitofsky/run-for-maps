import localFont from "next/font/local";

// Display face for the hero headline and the nav brand wordmark.
export const heroFont = localFont({ src: "../app/fonts/OrangeJuice.ttf", display: "swap" });
